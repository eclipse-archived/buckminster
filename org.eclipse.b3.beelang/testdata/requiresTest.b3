
unit a {
	requires a/b;
	requires a/b/1;
	requires a/b/1.2.3.4r34;
	// requires a; // uncomment for syntax error
	// requires a/b/; //uncomment for syntax error
	requires a/b/"";
	requires a/b/"1.2.3a&/()";
	
	requires a.b.c.d/x.y.z;
	requires a.b.c.d/x.y.z/"";
	requires a.b.c.d/x.y.z/"1..2..3<#>";

	requires a/b/[1];
	requires a/b/[1,2];
	requires a/b/[1.2.3.4r34,1.2.5];
	// requires a; // uncomment for syntax error
	// requires a/b/; //uncomment for syntax error
	requires a/b/[""];
	requires a/b/["1.2.3a&/()",2.3];
	
	requires a.b.c.d/x.y.z/[r];
	requires a.b.c.d/x.y.z/["",r];
	requires a.b.c.d/x.y.z/["1..2..3<#>",2];
	
	requires a/b/[1,2];
	requires a/b/(1,2];
	requires a/b/[1,2);
	requires a/b/(1,2);

	meta requires a/b;
	meta requires a/b/1;
	meta requires a/b/1.2.3.4r34;
	// meta requires a; // uncomment for syntax error
	// meta requires a/b/; //uncomment for syntax error
	meta requires a/b/"";
	meta requires a/b/"1.2.3a&/()";
	
	meta requires a.b.c.d/x.y.z;
	meta requires a.b.c.d/x.y.z/"";
	meta requires a.b.c.d/x.y.z/"1..2..3<#>";

	meta requires a/b/[1];
	meta requires a/b/[1,2];
	meta requires a/b/[1.2.3.4r34,1.2.5];
	// meta requires a; // uncomment for syntax error
	// meta requires a/b/; //uncomment for syntax error
	meta requires a/b/[""];
	meta requires a/b/["1.2.3a&/()",2.3];
	
	meta requires a.b.c.d/x.y.z/[r];
	meta requires a.b.c.d/x.y.z/["",r];
	meta requires a.b.c.d/x.y.z/["1..2..3<#>",2];
	
	meta requires a/b/[1,2];
	meta requires a/b/(1,2];
	meta requires a/b/[1,2);
	meta requires a/b/(1,2);

}